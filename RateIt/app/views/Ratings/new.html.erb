  <h2>Current Vehicle to Rate</h2>

  
 <%= form_for @rating do |r| %>
  <%= r.label :vehicle_rating %>
  <%= r.text_field :vehicle_ratings %> 
  <%= r.hidden_field :vehicle_id, value: current_user.id %>

  <%= r.submit "Submit" %>

 <%end%><br>

<%= link_to "Logout", logout_path %> <%= link_to 'Back', '/users/show' %>
